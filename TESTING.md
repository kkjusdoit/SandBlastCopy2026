# 道具与新机制 — 测试指南 (TESTING)

> 分支：`feature/props-and-mechanics`
> 覆盖：障碍块（磨损）、炸弹（静态地雷 / 天降变雷 / 天降即炸）
> 前置：用项目指定的 Unity 版本打开工程，进入 Play 模式，开一局（Playing 状态）。

## GM 快捷键（仅编辑器）

所有 GM 键都在 `#if UNITY_EDITOR` 下，**正式微信包会自动剔除**，不影响线上。
仅在**对局进行中（Playing）**生效。用纯字母键，避开已占用的 A/D/W/S/P 与系统 F 键。

| 键 | 作用 | 备注 |
|---|---|---|
| **F8** | 触发色彩挑战 | 项目原有，非本次新增 |
| **O** | 再撒一批障碍 | 数量 = `InitialObstacleCount` |
| **K** | 全场障碍各扣 1 hp | 一键看 红→琥珀→绿→破 全周期，不用凑真消除 |
| **L** | 清空所有障碍 | |
| **B** | 排队一个**天降炸弹方块**（变雷） | 下一块变炸弹，可移动/瞄准，落地变地雷（可拆可躲） |
| **V** | 排队一个**天降即炸炸弹** | 下一块变炸弹，可瞄准，**落地立即炸坑**，无 fuse 无拆弹 |
| **N** | 直接埋一颗**静态地雷** | 不用瞄准，直接落在场地中下部 |

每次操作会在 Console 打印状态（障碍数 / 炸弹放置结果）。

## 三种炸弹形态一览

| 形态 | 谁控制 | 落地行为 | 颜色 | 触发 |
|---|---|---|---|---|
| 静态地雷 | 场地（陷阱） | 已埋好，倒计时→炸坑 | 炭黑红脉冲 | 开局自带 / **N** |
| 天降变雷 | 玩家瞄准 | 落地变地雷，可拆可躲 | 炭黑红脉冲 | **B** |
| 天降即炸 | 玩家瞄准 | 落地立即炸坑 | 橙黄警告色 | **V** |

---

## 验收清单

### A. 障碍块 + 磨损
1. **开局观察**：中下部有没有 4 块红色障碍块。
2. **分流**：沙落下来会不会绕着障碍流动、堆出畸形三角（而不是平整堆积）。
3. **磨损色阶**：连按 3 次 **K**，确认障碍 红(hp3)→琥珀(hp2)→绿(hp1)→消失(hp0)。
4. **真实磨损**：**O** 撒满后正常玩，确认只有**紧挨着消除**的那圈障碍格掉血（边缘侵蚀），不是整块一次消失。
5. **连锁**：故意在障碍附近反复消除，磨穿开口后沙流入缺口，能否触发后续连锁消除。
6. **不堵死**：长时间玩，确认障碍不会把场地永久堵死。

### B. 静态地雷（开局 / N）
1. 开局有 1 颗地雷，炭黑红脉冲。
2. **倒计时**：每落一个新方块，fuse −1（越接近 0 越红）。
3. **引爆**：摆烂让它到 0，应炸出圆形坑洞、碎沙塌落，**不判负**。
4. 炸坑**不清障碍**（障碍只吃磨损），只清沙和其他炸弹。

### C. 天降变雷炸弹（B）
1. 按 **B**，下一块变成炭黑红脉冲的炸弹方块，Next 预览也显示为炸弹。
2. **瞄准**：能像普通方块一样左右移动/加速下落，把它扔到想要的位置。
3. **落地变雷**：落地后变成一颗地雷，开始 fuse 倒计时。
4. **拆弹奖励**：在地雷旁边凑一次左右贯通消除 → 地雷被拆除（不引爆）+ 额外加分（`BombDefuseReward × Combo`）。
5. **不拆**：不管它，倒计时到 0 正常炸坑。

### D. 天降即炸炸弹（V）
1. 按 **V**，下一块变成**橙黄警告色**的炸弹方块（与变雷的炭黑区分明显）。
2. **瞄准**：能移动/加速下落。
3. **落地即炸**：一落地立即炸坑，**不留地雷、无倒计时、不可拆**。
4. 炸完碎沙塌落，可能触发连锁消除。

---

## 可调参数（想改告诉我）

**障碍**（`Core/FlowSandBoard.cs` + `Runtime/FlowSandGameController.cs`）
- `InitialObstacleCount = 4` — 开局撒几块
- `ObstacleMaxHp = 3` — 几次消除磨穿

**炸弹**（`Core/FlowSandBoard.cs` + `Runtime/FlowSandGameController.cs` + `Runtime/FlowSandMatchCoordinator.cs`）
- `InitialBombCount = 1` — 开局埋几颗静态地雷
- `BombInitialFuse = 5` — 地雷倒计时（按"新方块数"计）
- `BombBlastRadius = 26` — 炸坑半径（grain 单位）
- `BombDefuseReward = 20` — 拆弹奖励基数（× Combo）

---

## 已知说明 / 打磨项

1. **Runtime 侧以 Unity 实际编译为准**：离线只用 `mcs` 校验了 Core（`FlowSandBoard`/`FlowSandTypes`）零错误零警告；Runtime 三个文件依赖 UI 包图，离线编不了。Play 时留意 Console 有无红字编译错误。
2. **炸弹脉冲动画**：目前借用消除时的 flash 开关，正常下落时 flash 不翻转，所以炸弹是"按 fuse 显示稳定红度"而非持续闪烁。要真正持续闪烁需接一个时间源，属打磨项。
3. **投放频率未定**：天降炸弹（B/V）目前只有 GM 键触发；正常对局里是否自动投放、频率多少，待验收手感后再定。
